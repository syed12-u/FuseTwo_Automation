# ---------------------------------------------------------------------------
# PRODUCTION / LIVE environment
# Non-secret host configuration. Committed to git.
#
# NOTE: confirm these hosts against the real production deployment before the
# first live run.
#
# Writes are OFF by default here: tests tagged @write create, edit or delete
# real advertiser data. To deliberately allow them for one run:
#   ALLOW_WRITE_TESTS=true npm run test:prod
# ---------------------------------------------------------------------------

APP_BASE_URL = https://advertiser.fusetwo.com
MANAGEMENT_BASE_URL = https://management.prod.fusetwo.com
CONTROL_BASE_URL = https://control.corp.flexoffers.com

ALLOW_WRITE_TESTS = false
